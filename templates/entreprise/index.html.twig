{% extends 'base.html.twig' %}

{% block contenu %}
<table class="table table-striped table-hover table-sm">
    <thead>
        <tr>
            <th>ENTREPRISE</th>
            <th>SIRET</th>
            <th>ADRESSE</th>
            <th>CP</th>
            <th>VILLE</th>
            <th>MODIFIER</th>
            <th>AJOUTER</th>
        </tr>
    <thead>
    <tbody>
    {% for entreprise in entreprises %}
    <tr>
        <td>
        {# <a href=" {{ path('entreprise_show', {'id':entreprise.id}) }}"> #}
        {{ entreprise.raisonSociale }}
        {# </a> #}
        </td>
        <td>{{ entreprise.siret }}</td>
        <td>{{ entreprise.adresse }}</td>
        <td>{{ entreprise.cp }}</td>
        <td>{{ entreprise.ville }}</td>
        {# <td><a href="{{ path('entreprise_edit', {'id':entreprise.id}) }}">Modifier</a></td>
        <td><a href="{{ path('entreprise_add', {'id':entreprise.id}) }}">Ajouter</a></td> #}
    </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}